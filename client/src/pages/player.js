import React from 'react';
import { Layout, PlayerContent} from '../components';


const PlayerStats = () => {
    return (
        <>
            <Layout />

            <PlayerContent />

        </>
    );
};

export default PlayerStats;